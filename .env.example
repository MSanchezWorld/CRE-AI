# ============================================================
# BorrowBot — Environment Variables
# Copy this file to .env and fill in your values.
# ============================================================

# --- Network ---
# Base mainnet RPC. The default works but may rate-limit under load.
# For production, use your own provider (Alchemy, Infura, QuickNode).
BASE_RPC_URL=https://base.drpc.org

# --- Deployment ---
# Private key for deploying contracts and funding the vault.
# Must have ETH (gas) + USDC (collateral) on Base.
DEPLOYER_PRIVATE_KEY=

# Optional: override the contract owner (defaults to deployer address).
OWNER_ADDRESS=

# --- Chainlink CRE ---
# Separate private key for CRE workflow broadcasts.
# SECURITY: Use a different key than DEPLOYER. Fund with minimal ETH (gas only).
CRE_ETH_PRIVATE_KEY=

# --- Web UI (Privy) ---
# Privy app ID for wallet connect. Get one at https://privy.io
NEXT_PUBLIC_PRIVY_APP_ID=

# --- Agent Server ---
# The agent HTTP server listens here (used by CRE workflow).
# HOST=127.0.0.1
# PORT=8787

# --- API Security ---
# Set these if you expose demo/agent routes in production.
# Routes check these via x-demo-runner-secret and x-agent-secret headers.
DEMO_RUNNER_SECRET=
AGENT_PLAN_SECRET=

# --- Feature Flags ---
# Keep these OFF in production. Only enable for local testing.
ENABLE_DEMO_RUNNER=false
ENABLE_RESET_RUNNER=false
